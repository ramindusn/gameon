// @gameon/supabase — Supabase client + generated types + auth helpers live here.
// Scaffold placeholder; the real client (with env + RLS-scoped queries) is wired
// in TASK-1.4 once the new Supabase project exists.

export const SUPABASE_CONFIGURED = false

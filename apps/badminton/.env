# Committed on purpose: these are the DEV project's values. The publishable key
# is PUBLIC by design (it ships in the client bundle; RLS guards the data), so a
# fresh clone runs with zero setup. DO NOT add secrets here (service key, DB
# passwords). Prod uses .env.production (git-ignored); CI uses GitHub secrets.
VITE_SUPABASE_URL=https://xlovjvvhsemqaqbknmyi.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_3WOU7PQUEQeEhf4PxdoH2g_L9KJNVNS

# Supabase (client) — safe to ship; RLS is the guard.
VITE_SUPABASE_URL=https://YOUR-REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# NOTE: the secret key (sb_secret_...) is server-only — never put it here.
# It lives as a Supabase Edge Function secret, not in the app.

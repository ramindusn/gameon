# Local development points at the shared DEV Supabase project (dev DB), so a
# fresh `cp .env.example .env && npm run dev` behaves like badmintonduo.pages.dev.
# The publishable key is safe to commit — Row-Level Security is the guard; the
# secret key lives only as a Supabase Edge Function secret, never here.
#
# Prod uses .env.production (not committed). See docs/RUNBOOK.md "Environments".
VITE_SUPABASE_URL=https://xlovjvvhsemqaqbknmyi.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_3WOU7PQUEQeEhf4PxdoH2g_L9KJNVNS
